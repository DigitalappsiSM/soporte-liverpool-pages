(()=>{const API="https://portal-soporte-liverpool.escanor-enrique.chatgpt.site/api/tickets",stores=window.LIVERPOOL_STORES||[],selected=new Set();let openedAt=Date.now();const q=s=>document.querySelector(s),form=q("#support-form"),requester=q("#requesterName"),storeInput=q("#store"),storeOptions=q("#store-options"),modelsSlot=q("#models-slot"),modelGrid=q("#model-grid"),modelsEmpty=q("#models-empty"),comments=q("#comments"),attachment=q("#attachment"),attachmentName=q("#attachment-name"),submit=q("#submit-button"),error=q("#form-error");for(const store of stores){const option=document.createElement("option");option.value=store.label;option.label=store.determinant;storeOptions.append(option)}const currentStore=()=>stores.find(item=>item.label===storeInput.value||item.determinant===storeInput.value);function chipClass(model){const key=model.toLowerCase().replaceAll(" ","-").replace("°","");return key.startsWith("vwpl")?"chip-vwpl":"chip-"+key}function renderModels(){const store=currentStore();selected.clear();modelGrid.replaceChildren();if(!store){modelsSlot.disabled=true;modelsSlot.classList.add("slot-disabled");modelsEmpty.hidden=false;modelGrid.hidden=true;return update()}modelsSlot.disabled=false;modelsSlot.classList.remove("slot-disabled");modelsEmpty.hidden=true;modelGrid.hidden=false;for(const model of[...store.models,"Otro"]){const button=document.createElement("button");button.type="button";button.className="model-chip "+chipClass(model);button.setAttribute("aria-pressed","false");button.innerHTML='<span class="chip-dot"></span>'+model+'<span class="chip-check">✓</span>';button.onclick=()=>{selected.has(model)?selected.delete(model):selected.add(model);button.classList.toggle("selected",selected.has(model));button.setAttribute("aria-pressed",String(selected.has(model)));comments.placeholder=selected.has("Otro")?"Describe el soporte no catalogado y la incidencia…":"¿Qué está ocurriendo? Incluye desde cuándo y qué se ha intentado…";update()};modelGrid.append(button)}update()}function update(){const store=currentStore(),length=comments.value.trim().length;q("#comment-count").textContent=length+"/12 mínimo";q("#comment-count").classList.toggle("warning",length>0&&length<12);q("#summary-requester").textContent=requester.value.trim()||"Sin solicitante";q("#summary-store").textContent=store?store.label:"Sin tienda";q("#summary-models").textContent=selected.size?[...selected].join(" · "):"Sin soporte";submit.disabled=!(requester.value.trim().length>=3&&store&&selected.size&&length>=12)}requester.oninput=update;comments.oninput=update;storeInput.oninput=renderModels;attachment.onchange=()=>{const file=attachment.files&&attachment.files[0];attachmentName.textContent=file?file.name:"Adjuntar evidencia"};form.onsubmit=async event=>{event.preventDefault();const store=currentStore();if(submit.disabled||!store)return;const file=attachment.files&&attachment.files[0];if(file&&file.size>10485760){error.textContent="El archivo no puede superar 10 MB.";error.hidden=false;return}error.hidden=true;submit.disabled=true;submit.innerHTML='Enviando a Odoo… <span>→</span>';const body=new FormData();body.set("requesterName",requester.value.trim());body.set("determinant",store.determinant);body.set("storeName",store.name);body.set("models",JSON.stringify([...selected]));body.set("comments",comments.value.trim());body.set("openedAt",String(openedAt));body.set("website",q("#website").value);if(file)body.set("attachment",file);try{const response=await fetch(API,{method:"POST",body}),result=await response.json();if(!response.ok||!result.folio)throw new Error(result.error||"No fue posible crear el ticket.");q("#form-content").hidden=true;q("#ticket-folio").textContent=result.folio;q("#success-state").hidden=false}catch(reason){error.textContent=reason instanceof Error?reason.message:"No fue posible crear el ticket.";error.hidden=false;submit.innerHTML='Enviar ticket <span>→</span>';update()}};q("#reset-button").onclick=()=>{form.reset();selected.clear();openedAt=Date.now();q("#success-state").hidden=true;q("#form-content").hidden=false;attachmentName.textContent="Adjuntar evidencia";renderModels()};update()})();
